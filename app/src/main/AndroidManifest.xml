<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="lat.trust.trustdemo"
    android:sharedUserId="trustResponse.trifles"
    android:sharedUserLabel="@string/shared_user_label">


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />



    <application
        android:name="lat.trust.trustdemo.TrustDemoApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name="lat.trust.trustdemo.ui.splash.SplashActivity"
            android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- FIREBASE SERVICE -->


        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="c2f2723ade81917cf6b8cac00d194cd208df72ad" />

    </application>

</manifest>